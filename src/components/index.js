import './index.css'

const SalaryRangeItem = props => {
  const {salaryRange} = props
  const {label} = salaryRange
  return (
    <div>
      <h1>vamshi</h1>
    </div>
  )
}

export default SalaryRangeItem
